version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
blocks:
  - name: Tests
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - rubocop
        - name: 'Job #2'
          commands:
            - apt install ruby-rspec-core
            - rspec
